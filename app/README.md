The Next.js app for the QUL Book-Spread Reader. Run it with `pnpm install && pnpm dev` from this directory.

Everything else — the Resources it renders, the licensing position, Docker and deployment — is in the [repo root README](../README.md).
